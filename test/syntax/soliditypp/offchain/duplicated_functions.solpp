pragma soliditypp >=0.4.2;

contract Test {
    uint a;
    function f() offchain view returns (uint) {
        return a;
    }

    function f() offchain view returns (uint) {
        return a;
    }
}
// ----
DeclarationError 1686: (60-127): Function with same name and parameter types defined twice.